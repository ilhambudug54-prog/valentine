<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Be My Valentine?</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: radial-gradient(circle at top, #ffb3c7, #ff6f91, #ff4d6d);
      min-height: 100vh;
      display: grid;
      place-items: center;
      color: #fff;
      overflow-x: hidden;
    }
    .card {
      background: rgba(0,0,0,.35);
      backdrop-filter: blur(8px);
      border-radius: 20px;
      padding: 20px;
      max-width: 420px;
      width: calc(100% - 24px);
      box-shadow: 0 20px 40px rgba(0,0,0,.25);
      text-align: center;
      animation: pop .6s ease;
    }
    @keyframes pop { from { transform: scale(.95); opacity: 0 } to { transform: scale(1); opacity: 1 } }

    h1 { margin: 8px 0 14px; letter-spacing: .3px; }
    .btns { display: flex; gap: 12px; justify-content: center; margin-top: 12px; }
    button {
      border: 0;
      border-radius: 999px;
      padding: 12px 18px;
      font-size: 16px;
      cursor: pointer;
      transition: transform .1s ease, box-shadow .1s ease, opacity .1s ease;
      box-shadow: 0 10px 20px rgba(0,0,0,.25);
    }
    .yes { background: #22c55e; color: #083b1f; }
    .yes:hover { transform: translateY(-1px); }
    .no {
      background: #ef4444;
      color: #3b0a0a;
      opacity: .6;
      pointer-events: none; /* No gak bisa dipencet */
      cursor: not-allowed;
    }

    .hidden { display: none; }

    .gallery {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
      margin: 12px 0;
    }
    .gallery img {
      width: 100%;
      height: 220px;
      object-fit: cover;
      border-radius: 14px;
      box-shadow: 0 8px 18px rgba(0,0,0,.25);
    }

    .heart {
      position: fixed;
      width: 14px;
      height: 14px;
      background: #ffccd5;
      transform: rotate(45deg);
      animation: float 6s linear infinite;
      opacity: .8;
    }
    .heart::before, .heart::after {
      content: "";
      position: absolute;
      width: 14px; height: 14px;
      background: #ffccd5;
      border-radius: 50%;
    }
    .heart::before { left: -7px; top: 0; }
    .heart::after { left: 0; top: -7px; }

    @keyframes float {
      0% { transform: translateY(100vh) rotate(45deg); }
      100% { transform: translateY(-10vh) rotate(45deg); }
    }

    .play {
      margin-top: 10px;
      background: #0ea5e9;
      color: #022c3a;
    }
    footer { font-size: 12px; opacity: .8; margin-top: 6px; }
  </style>
</head>
<body>

  <!-- Layar awal -->
  <div id="screen-ask" class="card">
    <h1>Wanna be my Valentine? 💘</h1>
    <div class="btns">
      <button class="yes" id="yesBtn">Yes</button>
      <button class="no">No</button>
    </div>
    <footer>(Tenang, tombol No emang gak bisa diklik 😜)</footer>
  </div>

  <!-- Layar setelah Yes -->
  <div id="screen-love" class="card hidden">
    <h1>Happy Valentine Day! ❤️</h1>
    <p><b>aku beruntung memilikimuuu</b></p>

    <div class="gallery">
      <img src="foto1.jpg" alt="Foto kita 1">
      <img src="foto2.jpg" alt="Foto kita 2">
    </div>

    <button class="play" id="playMusic">▶️ Play music</button>
    <footer>From me, with love ✨</footer>
  </div>

  <!-- Musik (masukin file MP3 kamu sendiri) -->
  <audio id="bgm" src="love-wave-to-earth.mp3" loop></audio>

  <script>
    const yesBtn = document.getElementById('yesBtn');
    const ask = document.getElementById('screen-ask');
    const love = document.getElementById('screen-love');
    const bgm = document.getElementById('bgm');
    const playBtn = document.getElementById('playMusic');

    yesBtn.addEventListener('click', () => {
      ask.classList.add('hidden');
      love.classList.remove('hidden');
      confettiHearts();
    });

    playBtn.addEventListener('click', async () => {
      try {
        await bgm.play();
        playBtn.textContent = "🎶 Playing...";
      } catch (e) {
        alert("Browser minta interaksi dulu. Coba klik Play lagi ya.");
      }
    });

    function confettiHearts() {
      for (let i = 0; i < 24; i++) {
        const h = document.createElement('div');
        h.className = 'heart';
        h.style.left = Math.random() * 100 + 'vw';
        h.style.animationDelay = Math.random() * 4 + 's';
        h.style.opacity = .6 + Math.random() * .4;
        document.body.appendChild(h);
        setTimeout(() => h.remove(), 7000);
      }
    }
  </script>
</body>
</html>